SUBDIRS = mopen generators
if INCLUDE_MANAGED_CODE
#don't include respack because the toplevel includes that already
SUBDIRS += xamlg xaml2html mxap munxap unsign
endif

EXTRA_DIST = launcher.in Options.cs README
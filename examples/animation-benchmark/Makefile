animation-benchmark: animation-benchmark.cpp
	gcc animation-benchmark.cpp -o animation-benchmark `pkg-config --libs --cflags gtk+-2.0 mozilla-gtkmozembed mozilla-js moon`

clean:
	rm -f animation-benchmark

